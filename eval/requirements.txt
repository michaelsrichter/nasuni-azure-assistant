# Microsoft Foundry evaluation harness for the Nasuni-on-Azure assistant.
# Uses the new Foundry evaluations API (OpenAI Evals surface) so runs appear in
# the New Foundry portal's Evaluations tab. Pin to a known-good range.
azure-ai-projects>=2.2.0
openai>=1.0.0
azure-identity>=1.21.0
requests>=2.32.0
python-dotenv>=1.0.1

# Web service that drives evaluations from the SPA (eval/service/app.py).
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
